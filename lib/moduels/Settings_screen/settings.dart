import 'package:flutter/cupertino.dart';

class settings extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('settings model',style: TextStyle(
        fontWeight: FontWeight.bold,
        fontSize: 20,
      ),),
    );
  }
}
